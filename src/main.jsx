import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.querySelector('#root')).render(
  <h1 style={{ textAlign: 'center' }}>
    Hello, world! I'm using React
  </h1>
);